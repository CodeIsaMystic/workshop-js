import { randomWholeNumber } from "./random-whole-number";

describe('randomWholeNumber', () => {
    test('randomWholeNumber should returns xxxx when xxxxx', () => {
        // Act 
        const number = xxxxx
        const expected = xxxxx

        // Arrange
        const testResult = randomWholeNumber()

        // Assert
        expect(testResult).toBe(expected)
    })

    test('randomWholeNumber should returns xxxxx when xxxxx', () => {
    })

    
    test('randomWholeNumber should returns xxxxx when xxxxx', () => {
    })

    test('randomWholeNumber should returns xxxxx when xxxxx', () => {
    })


 


})











// Une année bissextile

// une année divisible par 4,      /4
// mais non divisible par 100   !  /100   => except  /400


// sauf si elle est également divisible par 400.






// typical
// 2001 => `false`
// leap typical
// 1996 => `true`
// atypical
// 1900 => `false`
// leap atypical
// 2000 => ``true``